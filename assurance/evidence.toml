schema = "isometric-evidence/v1"

[[claim]]
id = "BOOT-DET-001"
statement = "The original bootstrap fixture renders identical indexed bytes across repeated calls in one pinned process."
evidence = ["cargo test -p isometric-render reference_render_is_byte_deterministic"]
status = "implemented"

[[claim]]
id = "BOOT-STYLE-001"
statement = "Every bootstrap pixel is an index into a palette containing at most 128 colors."
evidence = ["cargo test -p isometric-render every_pixel_is_in_the_palette", "cargo test -p isometric-style"]
status = "implemented"

[[claim]]
id = "BOOT-TRANSIENT-001"
statement = "The v1 semantic enum and committed style assets cannot represent a person or vehicle."
evidence = ["cargo test --workspace", "scripts/check-policy.sh"]
status = "implemented"

[[claim]]
id = "VS-QUAL-001"
statement = "The complete Stanford vertical slice satisfies all accepted qualification gates."
evidence = []
status = "not-implemented"

[[claim]]
id = "PROTO-CONTRACT-001"
statement = "The exact hero bounds, epoch, source roles, Google exclusion, visual comparison protocol, and stop conditions are versioned and policy-validated."
evidence = ["source.lock.json", "ADR 0004", "ADR 0005", "research/2026-08-17-prototype-source-contract.md", "python scripts/validate_manifests.py"]
status = "implemented"

[[claim]]
id = "PROTO-SOURCE-001"
statement = "Approved hero sources synchronize into a bounded-memory content-addressed cache only after exact length and SHA-256 verification."
evidence = ["cargo test -p isometric-source", "cargo run --locked -- source sync", "python scripts/validate_manifests.py"]
status = "implemented"

[[claim]]
id = "PROTO-SOURCE-RELIABILITY-001"
statement = "Locked HTTPS sources use bounded transient-only retries, entity-tag and range-guarded partial continuation, source-specific sanitized diagnostics, and exact-lock CI caching while permanent and integrity failures fail without retry."
evidence = ["cargo test -p isometric-source", "cargo test -p isometric-cli", ".github/workflows/scheduled-assurance.yml", "CI artifact perception-recompile-evidence"]
status = "implemented"

[[claim]]
id = "PROTO-FIXTURE-001"
statement = "Licensed portable fixtures cover polygon holes, building parts, roads, canopy, confidence, explicit unknowns, and fail-closed provenance."
evidence = ["fixtures/world/representative.json", "fixtures/world/invalid/missing-source-rights.json", "fixtures/world/invalid/unknown-source.json", "python scripts/validate_world_fixtures.py"]
status = "implemented"

[[claim]]
id = "PROTO-WORLD-001"
statement = "The Rust world model validates immutable polygonal geometry, semantic metadata, provenance, review state, origin, partitions, and integer screen bounds without transient variants."
evidence = ["cargo test -p isometric-world", "cargo clippy -p isometric-world --all-targets --all-features -- -D warnings", "fixtures/world/representative.json"]
status = "implemented"

[[claim]]
id = "PROTO-COMPILE-001"
statement = "Locked OSM, Overture, NAIP, and LiDAR evidence compile deterministically into an inspectable 2,820-object hero world with stable IDs, integer coordinates, named landmark evidence, 5,202 ppm explicit unknown coverage, and a verified source-to-world hash chain."
evidence = ["cargo test -p isometric-world hero_compile_is_deterministic_and_inspectable", "cargo run --release --locked -- world compile", "cargo run --locked -- world inspect", "world.manifest.json"]
status = "implemented"

[[claim]]
id = "PROTO-PERCEPTION-001"
statement = "The bounded safe-Rust perception compiler transforms exact locked NAIP and streamed LiDAR into a frozen model-free semantic artifact, masks vector-owned cells, excludes unclassified low elevated returns from persistent evidence, retains no source pixels or transient classes, and reduces hero unknown coverage below two percent."
evidence = ["cargo test -p isometric-perception", "fixtures/perception/hero-evidence.json", "perception.lock.json", "python scripts/validate_manifests.py", "research/2026-08-17-prototype-perception-evidence.md", ".github/workflows/scheduled-assurance.yml"]
status = "implemented"

[[claim]]
id = "PROTO-RASTER-001"
statement = "The safe-Rust raster core performs bounded fixed-point triangle fill, shared-edge ownership, integer depth, stable equal-depth tie-breaking, and palette-indexed output with exactly five pixel-buffer bytes per active pixel."
evidence = ["cargo test -p isometric-render raster::tests", "cargo clippy -p isometric-render --all-targets --all-features --locked -- -D warnings", "docs/chapters/rendering.md"]
status = "implemented"

[[claim]]
id = "PROTO-SCENE-001"
statement = "The canonical fused hero world renders as a deterministic 1,954 by 880 Stanford-shaped indexed preview with polygon holes, NAIP and LiDAR-backed terrain and canopy, hardscape classes, empty parking, flat roofs, and directional facades."
evidence = ["cargo test -p isometric-render hero_world_produces_deterministic_non_abstract_art", "cargo run --release --locked -- render region artifacts/render/hero.ppm", "render.manifest.json", "CI artifact deterministic-render-evidence"]
status = "implemented"

[[claim]]
id = "PROTO-TILE-001"
statement = "World-aligned guarded tiles reconstruct the monolithic hero byte for byte, reject unsafe requests, and render the approximately 8K probe with bounded per-tile pixel memory."
evidence = ["cargo test -p isometric-render guarded_tiles_reassemble_to_the_exact_full_scene", "cargo test -p isometric-render guarded_tile_is_deterministic_and_rejects_unsafe_requests", "cargo test --release -p isometric-render eight_k_tile_set_is_deterministic_and_bounded -- --ignored --nocapture", "docs/chapters/performance.md"]
status = "implemented"

[[claim]]
id = "PROTO-GRAMMAR-001"
statement = "The versioned style pack deterministically controls faceted vegetation, hard shadows, palette-only one-pixel outlines, and tile-independent world-anchored material patterns."
evidence = ["cargo test -p isometric-style", "cargo test -p isometric-render scene::tests", "styles/stanford_v1/style.toml", "docs/chapters/rendering.md", "CI artifact deterministic-render-evidence"]
status = "implemented"

[[claim]]
id = "PROTO-LANDMARK-001"
statement = "Original parameterized geometry gives Hoover Tower, Memorial Church, and the Main Quad distinct deterministic silhouettes without copied textures or manually painted pixels."
evidence = ["cargo test -p isometric-render all_three_landmark_grammars_emit_distinct_details", "cargo test -p isometric-render hero_world_produces_deterministic_non_abstract_art", "styles/stanford_v1/style.toml", "docs/chapters/art-system.md", "CI artifact deterministic-render-evidence"]
status = "implemented-not-approved"

[[claim]]
id = "PROTO-STYLE-A-001"
statement = "Candidate A deterministically produces four native scenes, three isolated landmark masks, a contact sheet, indexed metrics, and explicit deviations without copied reference imagery, generated final pixels, or manual tile painting."
evidence = ["cargo run --release --locked -- style candidate-a artifacts/style/candidate-a", "research/2026-08-17-style-candidate-a.md", "CI artifact deterministic-render-evidence"]
status = "implemented-not-approved"

[[claim]]
id = "PROTO-STYLE-B-001"
statement = "Candidate B adds deterministic ordinary facade, convex roof, canopy, material, and parking grammar while preserving Candidate A bytes and exact guarded-tile reconstruction."
evidence = ["cargo test -p isometric-style candidate_b_is_valid_and_enables_bounded_details", "cargo test -p isometric-render candidate_b_adds_deterministic_ordinary_detail", "cargo test -p isometric-render guarded_tiles_reassemble_to_the_exact_full_scene", "cargo run --release --locked -- style candidate-b artifacts/style/candidate-b", "research/2026-08-17-style-candidate-b.md", "CI artifact deterministic-render-evidence"]
status = "implemented-not-approved"

[[claim]]
id = "PROTO-STYLE-C-001"
statement = "Candidate C adds deterministic complex-roof surface treatment, object-stable facade variation, landmark materials, and semantic circulation accents while preserving Candidates A and B exactly."
evidence = ["cargo test -p isometric-style candidate_c_is_valid_and_enables_final_bounded_details", "cargo test -p isometric-render candidate_c_adds_deterministic_bounded_style_treatment", "cargo test -p isometric-render guarded_tiles_reassemble_to_the_exact_full_scene", "cargo run --release --locked -- style candidate-c artifacts/style/candidate-c", "research/2026-08-17-style-candidate-c.md", "CI artifact deterministic-render-evidence"]
status = "implemented-not-approved"

[[claim]]
id = "PROTO-WEB-POLICY-001"
statement = "The OpenSeadragon viewer derives bounded decoded-tile cache counts from explicit mobile and desktop byte budgets and gives phone-width screens a legible initial view without changing Home behavior."
evidence = ["npm --prefix web test -- --run", "web/src/viewer-policy.test.ts", "docs/chapters/web-delivery.md"]
status = "implemented"

[[claim]]
id = "PROTO-DZI-001"
statement = "The canonical Rust publisher creates an atomic lossless WebP DZI from bounded guarded tiles, records the exact style identity, retains indexed canonical levels, and validates completeness, palette fidelity, style identity, and the full artifact hash chain."
evidence = ["cargo test -p isometric-publish", "cargo run --release --locked -- publish dzi", "cargo run --release --locked -- validate release", ".github/workflows/scheduled-assurance.yml", "docs/chapters/performance.md"]
status = "implemented"

[[claim]]
id = "PROTO-ASSURANCE-001"
statement = "Three explicit Candidate C DZI publications are byte-identical under one release binary, stay below the prototype RSS and duration ceilings, exceed the accepted throughput floor, and load within hosted mobile and desktop request and cache budgets."
evidence = ["python3 scripts/test_measure_prototype.py", "python3 scripts/measure_prototype.py --binary target/release/isometric-stanford --output artifacts/assurance/prototype", "web/e2e/viewer.spec.ts", ".github/workflows/scheduled-assurance.yml", "docs/chapters/performance.md", "docs/chapters/web-delivery.md"]
status = "implemented"

[[claim]]
id = "PROTO-VIEWER-001"
statement = "The bounded OpenSeadragon Canvas viewer loads lossless DZI artwork accessibly on desktop and mobile, retries tiles, exposes recoverable failures, and redraws after drawing-context restoration."
evidence = ["npm --prefix web run test:e2e", "web/e2e/viewer.spec.ts", ".github/workflows/scheduled-assurance.yml", ".github/workflows/release-dry-run.yml", "docs/chapters/web-delivery.md"]
status = "implemented"

[[claim]]
id = "PROTO-PR-GATE-001"
statement = "Hosted ci-pass rejects pull requests without a conventional title, required evidence sections, a linked issue, and exactly one release-impact label."
evidence = ["python scripts/test_validate_pr.py", "scripts/validate_pr.py", ".github/workflows/ci.yml"]
status = "implemented"

[[claim]]
id = "PROTO-PREVIEW-001"
statement = "A portable browser preview can be assembled only from the current verified Candidate C world and DZI bytes, is visibly marked unqualified, and cannot claim publication or final approval."
evidence = ["python scripts/test_assemble_preview.py", "scripts/assemble_preview.py", "web/src/release-metadata.test.ts", ".github/workflows/release-dry-run.yml", "research/2026-08-17-prototype-qualification.md"]
status = "implemented"

[[claim]]
id = "PROTO-REVIEW-NAV-001"
statement = "The exact Candidate C artifact exposes deterministic deep-linked landmark review crops with browser history, fail-closed release identity checks, desktop and mobile screenshots, and a complete no-scroll Pixel 7 layout."
evidence = ["npm --prefix web test -- --run", "npm --prefix web run test:e2e", "web/src/review-views.test.ts", "web/e2e/viewer.spec.ts", ".github/workflows/scheduled-assurance.yml", "docs/chapters/web-delivery.md"]
status = "implemented"

[[claim]]
id = "PROTO-QUAL-001"
statement = "The complete Stanford hero prototype satisfies all accepted qualification gates."
evidence = []
status = "not-implemented"

[[claim]]
id = "REF-CONTRACT-001"
statement = "Registered reference bundles fail closed unless color, whitebox, linear depth, view normals, fixed shadows, and coverage share one bounded orthographic grid, pass the pilot coverage floor, and match their exact content hashes."
evidence = ["cargo test -p isometric-reference", "cargo test -p isometric-cli reference_inspection_fails_closed_without_a_manifest", "docs/adr/0006-registered-reference-stylization.md"]
status = "implemented"

[[claim]]
id = "REF-CAPTURE-001"
statement = "The registered capture workspace holds one orthographic camera across six passes, fails closed on unstable or failed tile loading, keeps credentials out of artifacts and diagnostics, and atomically promotes only a Rust-validated complete bundle."
evidence = ["npm --prefix capture run check", "npm --prefix capture test", "npm --prefix capture run test:e2e", "capture/specs/hoover-pilot.json", "capture/specs/hoover-camera-probe.json", "docs/chapters/reference-capture.md", ".github/workflows/ci.yml"]
status = "implemented"

[[claim]]
id = "REF-REVIEW-001"
statement = "The routed reference workbench verifies the complete six-layer hash and registration chain before display, decodes only the selected comparison pair, synchronizes source-pixel navigation, and fails closed on corrupt or missing artifacts on desktop and mobile."
evidence = ["npm --prefix web test -- --run", "npm --prefix web run test:e2e", "web/src/reference-bundle.test.ts", "web/e2e/review.spec.ts", "docs/chapters/visual-qualification.md"]
status = "implemented"

[[claim]]
id = "MASK-CONTRACT-001"
statement = "Registered semantic mask artifacts use a stable 24-class ontology, exact reference hash chain, fixed-width confidence and instance records, bounded streaming validation, deterministic bytes, and structural rejection of transient or source-artifact pixels in persistent output."
evidence = ["cargo test -p isometric-mask", "cargo test -p isometric-cli mask_inspection_validates_an_immutable_synthetic_artifact", "docs/chapters/perception.md", "docs/chapters/no-transients.md"]
status = "implemented"

[[claim]]
id = "MASK-GEOMETRY-001"
statement = "Safe bounded Rust kernels extract deterministic depth and normal boundaries, morphology, components, distance, watershed partitions, and quantized line evidence while preserving the whole-supertile connectivity boundary and finite-radius guarded-crop equality."
evidence = ["cargo test -p isometric-mask geometry::tests", "fixtures/masks/geometry/opencv-oracle.json", "research/geometry/generate_opencv_oracle.py", "docs/chapters/perception.md"]
status = "implemented"
