# Accepted goldens

Only original Isometric Stanford outputs may be committed here. Every golden
must link its approved style issue, input fixture, renderer version, canonical
platform, palette, comparison tolerance, and reviewer.

The bootstrap uses a byte hash in Rust tests rather than committing a raster.
No style candidate has been accepted yet.
